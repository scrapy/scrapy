from twisted.internet.test.test_base import ThreadedResolverTests

class IPv6ThreadedResolverTest(ThreadedResolverTests):
    """
    Tests for ThreadedResolver imported from twisted,
    as we should support the same interface.
    """

class CachingThreadedResolverTest(ThreadedResolverTests):
    """
    Tests for ThreadedResolver imported from twisted,
    as we should support the same interface.
    """
