def take_first(values):
    for value in values:
        if value is not u'':
            return value


def return_values(values):
    return values


def join_strings(values):
    return u' '.join(values)

