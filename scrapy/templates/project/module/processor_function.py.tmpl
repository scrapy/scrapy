#Define your processor functions for feedexport. Used in conjuncture with _process_item/_import_processor,
#which takes an item and a list of processor functions as arguments. 

def processor_function(item, spider=None, **kwargs):
    # Add code to process the item data before export.
    # Optionally, use the spider instance if needed
    return modified_item